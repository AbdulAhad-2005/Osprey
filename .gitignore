# Python
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
.env
.env.local
*.db

# Node
node_modules/
.next/
out/
coverage/

# Build artifacts
build/
dist/
*.egg-info/

# OS and editor
.DS_Store
Thumbs.db
.vscode/*.log

.uv-cache
.agents
.claude/

backend/tests/
backend/uv.lock/
uv.lock
cli/tests

# Local engagement artifacts (sensitive)
reports/

# Third-party reference clones — never shipped; attribution in THIRD_PARTY_NOTICES.md
other tools/
hexstrike-ai/
strix/
shannon/

# Learned-skills tier — operator-local, never shipped/synced
skills/learned/
# Operator profile — the human operator's preferences, operator-local, never shipped/synced
skills/operator/
# FP-cache — operator-local noise-pattern judgments, may embed engagement detail
fp_cache/

# Internal audit notes — documents live/unpatched security gaps; keep local
# until fixed, never in shared history
docs/AUDIT_FINDINGS_LOG.md

mcp-config.json
mcp.json

# Committed test artifact — should not be tracked
test.db

# Real engagement evidence dumps (scraped target data) — operator-local,
# never shipped/synced; same reasoning as reports/ above
mcp-servers/*_Evidence/

# Redundant archive of plans/harness/ (the directory is the source of truth)
plans/harness.zip

# Scratch chat-log dump, not project documentation
opencode_chatgpt_discussion.txt
prompts.md
plans/harness/
