# Exact-pinned — see mcp-servers/requirements.txt's comment: this package
# previously drifted across 5 independent constraints in this repo. Bump ALL
# FIVE (this file, mcp-servers/requirements.txt, backend/requirements.txt,
# cli/pyproject.toml, kali-tools/Dockerfile) together, never just one.
mcp==1.28.1
httpx>=0.27.0
