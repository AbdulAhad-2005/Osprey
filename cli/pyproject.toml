[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pentest-cli"
version = "0.1.0"
description = "CLI interface for the Autonomous AI Pentesting Platform"
license = {text = "AGPL-3.0-or-later"}
requires-python = ">=3.11"
dependencies = [
    "prompt_toolkit>=3.0,<4.0",
    "httpx>=0.27,<1.0",
    "rich>=13.0,<14.0",
    "python-dotenv>=1.0,<2.0",
]

[project.scripts]
pentest = "cli.main:main"

[tool.setuptools]
# The importable `cli` package lives one level above this pyproject (repo root).
# Rooting the package dir at the parent makes `pip install -e ./cli` register
# the repo root on sys.path, so both `python -m cli` (from anywhere) and the `pentest` console script work
package-dir = {"" = ".."}

[tool.setuptools.packages.find]
where = [".."]
include = ["cli*"]
