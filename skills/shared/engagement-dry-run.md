# Engagement dry-run (soft instincts — not a gate)

Authorized targets only. This is a **wide** reminder of what elite looks like,
not a checklist the platform enforces and not a tool order.

Skip, reorder, invent — evidence decides.

## Freedom first

- Gaps, jobs lines, and finalize are **data / referee**, not a script.
- Prefer `platform_context` + your judgment over any skill file.
- Custom `platform_script` + `REL|` / `FINDING|` / `graph_link` are first-class.

## Useful moves (when the surface asks for them)

| Instinct | Why it helps |
|----------|----------------|
| Sisters / second enum | Thin inventory often lies |
| Live probe in parallel | Names without HTTP are unfinished questions |
| Jobs for slow tools | Chat stays useful while Kali works |
| Crown jewels + script depth | Titles ≠ app understanding |
| Graph links / tags | You name the story; memory keeps it |
| Report outline + finalize | Proof grades before COMPLETE language |

## Honest close

1. `platform_findings` / `platform_report_outline` — what is actually stored?  
2. `platform_finalize_check` — honor `partial_only` vs `complete`  
3. Soft warnings (hypothesis paths) → label as hypotheses, don’t inflate  

If finalize is BLOCKED, deepen **or** ship PARTIAL. Never invent COMPLETE from chat.

## Anti-narrow

Do **not** treat this file as stages. Do **not** re-run the same failing tool.
Do **not** widen to full port range without asking. Do invent when the catalog is thin.
