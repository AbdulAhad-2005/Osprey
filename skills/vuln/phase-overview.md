# Vulnerability assessment methodology (optional)

Prioritize by exposure from graph evidence — not a blind scanner checklist.
Not a mandatory stage.

## Operate

1. `platform_context()` for gaps + inferred focus
2. Targeted `platform_exec` / `platform_shell` — avoid noisy fan-out
3. Tag confidence honestly; exploitation needs explicit approval
